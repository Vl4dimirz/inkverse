node_modules/
# Android build outputs / local config (keep source, ignore generated)
android/.gradle/
android/build/
android/app/build/
android/app/release/
android/local.properties
android/.idea/
android/captures/
*.apk
*.aab
*.keystore
*.jks
# Exception: the fixed debug keystore MUST be committed for stable signatures.
# (Debug keystores are not secret — password is the public "android".)
!android/app/debug.keystore
android/app/google-services.json
